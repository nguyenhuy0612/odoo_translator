# Core dependencies
polib>=1.2.0

# Language Detection - Best accuracy with Lingua-py
# Lingua-py: Best for short text (single words, phrases) - RECOMMENDED
lingua-language-detector>=2.0.2

# FastText: Fallback if Lingua not available
# Note: Requires Python 3.12 or lower (3.13 not yet supported)
fasttext-wheel==0.9.2
numpy>=1.24.0,<2.0

# AI Translation
google-generativeai>=0.3.0

# Modern UI
customtkinter>=5.2.0
